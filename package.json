{
  "name": "azik-food-exchange",
  "version": "1.0.0",
  "description": "Gıda çalışanları için yemek takası platformu",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "vercel-build": "npm run install-all && npm run build",
    "render-build": "cd server && npm install && cd ../client && npm install && npm run build",
    "netlify-build": "npm install && cd client && npm install && CI=false npm run build && cd ../server && npm install",
    "start": "cd server && npm start",
    "netlify-start": "cd server && npm start"
  },
  "keywords": ["food", "exchange", "gıda", "takas"],
  "author": "Azık Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "build": "cd client && npm run build",
  "vercel-build": "npm run install-all && npm run build",
  "homepage": "."
}
