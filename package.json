{
  "name": "azik-food-exchange",
  "version": "1.0.0",
  "description": "Gıda çalışanları için yemek takası platformu",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "vercel-build": "npm run install-all && npm run build",
    "render-build": "cd server && npm install && cd ../client && npm install && npm run build",
    "netlify-build": "npm install && cd client && npm install && CI=false npm run build && cd ../server && npm install && cd ../netlify/functions && npm install",
    "start": "cd server && npm start",
    "netlify-start": "cd server && npm start"
  },
  "keywords": ["food", "exchange", "gıda", "takas"],
  "author": "Azık Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "express": "^4.18.2",
    "serverless-http": "^3.2.0",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.7",
    "uuid": "^9.0.1",
    "validator": "^13.11.0",
    "firebase-admin": "^12.0.0"
  },
  "build": "cd client && npm run build",
  "vercel-build": "npm run install-all && npm run build",
  "homepage": "."
}
